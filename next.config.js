/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['s.yimg.com'],
  },
  // logging: {
  //   fetches: {
  //     fullUrl: true,
  //   },
  // },
  // reactStrictMode: false
};

module.exports = nextConfig;
